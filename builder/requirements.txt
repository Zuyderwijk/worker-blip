# Required Python packages for optimized BLIP2 handler
# Locked versions for stability and compatibility

# Core ML dependencies  
torch==2.0.1                 # Verified compatible with lavis
torchvision==0.15.2
transformers==4.38.2         # Compatible with BLIP2 and lavis

# Image processing
pillow==10.1.0

# RunPod serverless
runpod>=1.0.0,<2.0.0

# LAVIS for BLIP2 - stable PyPI version
salesforce-lavis==1.0.2

# NumPy compatibility fix for LAVIS
numpy<2

# Explicit LAVIS dependency
opencv-python-headless==4.5.5.64